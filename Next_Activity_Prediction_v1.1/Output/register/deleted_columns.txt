Colonne eliminate per numero di 0 > 66%:
case:Cost Type

Colonne eliminate per numero di NaN > 5%:
lifecycle:transition
id
org:role

Colonne eliminate con stesso valore per ogni riga:


Colonne che non sono state selezionate:


